services:
  tunnel:
    build: .
    image: rpt-robot:latest
    container_name: rpt-robot
    restart: unless-stopped
    # network_mode: host — чтобы localhost:22 в туннеле указывал на sshd самого робота.
    network_mode: host
    volumes:
      - /etc/rpt-robot:/data
